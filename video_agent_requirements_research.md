# 视频理解 Agent 需求调研文档

生成日期：2026-04-28

## 1. 文档目的

本文档汇总视频理解 Agent 项目的需求调研结果，完整记录项目背景、用户目标、使用场景、关键需求、设计判断、第一版范围、失败处理、安全边界、验收标准和后续版本规划。

本文档可作为后续进入产品设计、技术方案设计、Cursor 或 Claude Code 项目模板搭建、脚本开发、测试验收的基础输入。

## 2. 项目背景

用户希望构建一个能够帮助高效率消化视频内容的 Agent。该 Agent 需要能够看视频、理解视频、建立索引，并围绕视频内容支持持续追问。

项目的核心出发点不是生成一次性摘要，而是建立一种可追溯、可复盘、可继续追问的视频理解工作流。

用户的原始设想包括三点：

1. 让 Agent 看视频，提炼视频完整脉络与大纲，筛选关键信息，帮助用户快速建立对视频内容的认识，抓住重点。
2. 当用户对第一轮提取出来的信息中的特定内容展开追问时，Agent 能从视频中筛选相关详细内容进行说明和讨论，并告知相关片段在完整视频中的位置。
3. 最终协助用户高效率消化视频中包含的有价值信息。

## 3. 目标场景

### 3.1 优先处理的视频类型

第一版优先覆盖以下三类视频：

| 类型 | 优先级 | 说明 |
|---|---:|---|
| 知识类长视频 | P0 | 课程、讲座、播客、访谈 |
| 实操类视频 | P0 | 教程、软件操作、代码演示、游戏开发过程 |
| 信息类视频 | P1 | 新闻、行业分析、产品发布、测评 |

其中知识类长视频和实操类视频最常见，应作为第一版主要测试对象。

### 3.2 视频来源

第一版主要处理：

| 来源 | 第一版定位 |
|---|---|
| 本地视频文件 | 必须支持 |
| 在线公开视频链接 | 先做可行性调研 |
| 私有平台视频 | 不作为第一版自动化重点 |
| 公司内部视频 | 可处理，但默认本地优先 |

### 3.3 输入方式

第一版必须支持本地视频文件输入。

在线视频希望未来支持直接输入链接自动处理，但由于平台限制、字幕获取不稳定、登录权限、地区限制、反爬和 DRM 等因素，第一版只做可行性调研，不把所有公开视频链接的自动处理作为刚性承诺。

## 4. 产品目标

### 4.1 总目标

构建一个能够围绕单个视频完成完整理解、结构化索引、重点提取、片段定位和后续追问的视频理解 Agent。

### 4.2 用户价值

该 Agent 应帮助用户实现：

1. 快速知道视频主要讲什么。
2. 识别视频中是否存在高价值信息。
3. 理解视频的完整结构和脉络。
4. 在追问时快速定位相关片段。
5. 根据视频内容获得准确、高质量回答。
6. 在必要时引入可靠外部信息辅助理解和校验。
7. 将视频信息沉淀为可追溯、可复盘、可继续的文档和索引资产。

## 5. 核心需求

### 5.1 第一轮输出

第一轮输出采用极简价值摘要加完整大纲的方式。

第一轮应包含：

1. 视频主题
2. 高价值信息判断
3. 高价值信息分布
4. 完整但简洁的大纲
5. 必要时间戳
6. 可追问方向提示

第一轮不需要帮用户判断视频是否值得看，只需要说明视频是否包含高价值信息，以及高价值信息主要集中在哪些方向。

完整大纲需要按视频原本脉络拆分章节，每章只概括核心内容。含有高价值信息的章节需要做简略标注，但不在第一轮展开全部细节。

### 5.2 时间戳策略

第一轮时间戳采用自动策略：

| 视频情况 | 时间戳策略 |
|---|---|
| 长视频 | 尽量给章节起止时间 |
| 短视频 | 可只给关键片段时间 |
| 信息密度高的视频 | 优先标出高价值片段位置 |
| 实操类视频 | 优先标出关键操作步骤的位置 |
| 用户追问时 | 必须能定位到相关视频片段 |

### 5.3 分析对象

Agent 需要根据视频类型自动判断重点，覆盖以下信息源：

1. 语音
2. 字幕
3. 画面
4. 屏幕文字
5. PPT 文字
6. 代码
7. 软件界面
8. 操作步骤

V2-lite 第一版优先处理语音和字幕。画面理解、OCR、抽帧和屏幕文字分析进入后续版本。

### 5.4 追问回答方式

追问时采用视频优先、必要时外部补充的策略。

规则如下：

1. 视频内容足够回答时，优先基于视频内容回答。
2. 视频内容不足、有疑点或需要校验时，搜索与问题直接相关的可靠资料。
3. 外部资料只用于补足或校验当前问题，不主动扩展到相邻话题。
4. 回答中需要明确区分视频内容和外部补充。
5. 非必要不扩展话题。

### 5.5 追问定位粒度

追问回答应包含：

1. 相关视频片段时间段
2. 片段内容概括
3. 为什么该片段支撑回答

对于实操类视频，如果画面信息重要，需要补充画面中发生了什么。V2-lite 第一版先依赖转录文本和索引完成，画面核对能力进入后续版本。

### 5.6 原文保留规则

重要定义、方法、观点、数据需要保留原文要点，但不长段引用。

规则如下：

1. 重要内容记录原文要点。
2. 原文只保留关键短句或核心表达。
3. 中文解释负责说明含义、上下文和用途。
4. 第一轮索引保留足够线索，便于后续追问定位和展开。
5. 不默认输出大段原文，避免笔记臃肿。

## 6. 高价值信息判断机制

### 6.1 动态校准

高价值信息判断采用动态校准机制。

在用户还不了解视频内容时，Agent 不能直接假设什么最有价值，需要先根据手边可拿到的可靠背景信息做定性判断，并与用户确认筛选目标视频中高价值信息的导向，作为初始要求。

可用背景信息包括：

1. 视频标题
2. 视频简介
3. 作者或频道信息
4. 视频类型
5. 公开视频下的章节、评论、简介链接
6. 用户给出的当前任务背景

### 6.2 初始校准流程

初始校准采用两步式：

1. 先问一个核心问题：用户最关心这个视频的什么。
2. 根据用户回答，结合视频内容本身的信息量、结构清晰度、松散程度和潜在价值方向，选择下一步方式。

下一步方式有两种：

1. 视频结构清晰、信息密度适中时，由 Agent 拟定默认高价值方向，让用户确认或修改。
2. 视频信息密集、结构松散、存在多个价值方向时，给出 3 到 5 个高价值方向，让用户选择或删改。

### 6.3 过程中动态修正

在提取信息、用户追问和讨论过程中，Agent 需要根据实际讨论内容做必要修正。

修正规则：

1. 用户追问频繁涉及的主题应提高索引权重。
2. 新发现的高价值信息需要补充到索引。
3. 原有判断不准确时，需要更新索引并留下修改记录。
4. 同一视频后续问答应继承最新筛选标准。

## 7. 分层交付

项目采用分层交付思想。最终停留在哪个阶段由具体分析任务情境与用户实际需求决定。每个节点都要留下与之相符的交付物，确保未来可追溯、可复盘、可继续。

### 7.1 状态定义

| 阶段 | 含义 | 交付物 |
|---|---|---|
| 可分析状态 | Agent 已拿到足够材料，可以开始处理 | 视频来源、可用材料、缺失材料、可处理性判断 |
| 初始校准完成 | Agent 已确定初始分析导向 | 用户关注点、初始高价值导向、推荐处理方案、能力调用建议 |
| 第一轮索引完成 | Agent 已完整扫过视频并建立可追问索引 | 极简价值摘要、完整大纲、高价值片段标记、Markdown 笔记、YAML 索引、JSON 索引 |
| 深度消化完成 | 用户围绕重点追问，Agent 已展开讲清关键内容 | 追问问题、相关片段、片段概括、支撑理由、必要外部补充、理解结论 |
| 任务闭环完成 | 视频信息转化为可使用成果 | 行动清单、学习笔记、决策参考、复盘结论、待验证问题 |

## 8. 外部信息使用规则

### 8.1 使用边界

外部信息可以用于：

1. 事实校验
2. 补足背景知识
3. 评价视频观点可靠性
4. 识别视频观点局限

外部信息必须和当前追问直接相关，且明确标注来源与用途。

默认不引入低置信度信息，不扩展到相邻话题。

### 8.2 可靠性优先

回答的可靠性和置信度是最高优先级。

Agent 默认参考新闻核查与科研文献判断中的成熟原则：

1. 优先使用一手来源、官方文档、原始论文、权威机构、主流专业媒体。
2. 谨慎使用自媒体、搬运内容、匿名爆料和无出处二次解读。
3. 默认排除来源不明、无法交叉验证、明显营销化的信息。
4. 涉及产品、政策、价格、版本、人物、事件时，需要复核时效性。
5. 单一说法不足以支撑关键结论时，应交叉验证。

### 8.3 不确定性提示

默认筛除低可靠信息，只在必要时提示不确定性。

需要提示不确定性的情况包括：

1. 视频内容本身含糊。
2. 视频说法与外部资料冲突。
3. 可靠来源之间存在差异。
4. 只能找到低质量或间接来源。
5. Agent 做了推断，而不是直接复述视频或资料。

### 8.4 视频忠实度

当视频中的某个说法可能有问题，但用户问的是视频里怎么讲时，Agent 应先忠实复述视频说法，再单独提示可能问题。

处理顺序：

1. 视频里怎么说。
2. 视频用什么证据或例子支撑。
3. 相关片段在哪里。
4. 哪些说法可能不准确。
5. 哪些地方缺少证据。
6. 哪些地方需要外部资料校验。

## 9. 输出语言与格式

### 9.1 输出语言

处理英文或其他语言视频时，采用保留原文术语，解释用中文的策略。

规则如下：

1. 专业术语、产品名、模型名、方法名尽量保留原文。
2. 术语含义、上下文、作用机制用中文解释。
3. 关键原文表述可按需保留，但第一轮默认不大量摘录。
4. 如果视频原文表述影响理解或后续检索，应记录在索引中。
5. 追问时可展开对应原文、中文解释和片段位置。

### 9.2 标准交付文件

每个视频处理流程应形成以下核心文件：

```text
video_project/
  input/
    video.mp4

  transcript/
    transcript.srt
    transcript.json

  index/
    video_index.yaml
    video_index.json

  notes/
    video_note.md

  logs/
    process_log.md
    failure_log.json
    index_change_log.md
```

## 10. 索引策略

### 10.1 索引格式

采用 YAML 主索引加 JSON 自动导出。

| 文件 | 作用 |
|---|---|
| video_index.yaml | 主索引，给人和 Agent 读写 |
| video_index.json | 派生索引，给脚本和后续工具调用 |
| video_note.md | 人类阅读版视频笔记 |

第一版规则：

```text
video_index.yaml 作为唯一人工维护源文件
video_index.json 由脚本自动从 YAML 生成
```

### 10.2 索引粒度

采用混合粒度。

| 内容类型 | 索引粒度 |
|---|---|
| 普通章节 | 章节级或小节级记录 |
| 高价值内容 | 片段级记录 |
| 实操内容 | 按操作步骤切分 |
| 信息密集内容 | 按观点、数据、案例、方法拆分 |
| 后续追问发现的重要内容 | 回写到索引 |

### 10.3 索引结构原则

1. 全片不遗漏结构。
2. 重点内容细粒度。
3. 普通内容低成本记录。
4. 索引可被追问调用。
5. 真实视频测试后调整字段。

### 10.4 索引更新规则

索引默认在原有文件上增量更新。

规则如下：

1. `index/video_index.yaml` 作为主索引。
2. 追问后发现新增信息、补充标签、补充片段说明时，直接在原文件中增量更新。
3. 不默认每次更新就创建新版本文件。
4. 如果修改已有判断、删除旧信息、重写某段摘要、调整高价值标记，需要写入修改记录。
5. 是否新开文件由用户决定。
6. 只有用户明确要求保留独立版本、做分支分析、冻结某个阶段结果时，才创建新文件。

### 10.5 修改记录

`logs/index_change_log.md` 记录非增量修改。

建议字段：

```yaml
change_record:
  time:
  changed_section:
  change_type: incremental_addition | correction | deletion | rewrite | value_reclassification
  reason:
  source:
  related_question:
  before:
  after:
```

## 11. 自动支持追问

### 11.1 能力定义

自动支持追问指的是：第一轮视频索引完成后，用户提出后续问题时，Agent 能自动利用已有索引和相关材料完成定位、检索、必要核对和回答，不需要用户重新手动告诉它该看视频哪一段。

### 11.2 能力层级

| 层级 | 能力 |
|---|---|
| 层级 1 | 能查索引，回答大多数概括性问题 |
| 层级 2 | 能定位具体片段，并说明支撑理由 |
| 层级 3 | 必要时能回看转录文本、截图、OCR、抽帧信息核对细节 |
| 层级 4 | 追问后还能自动修正和补充索引 |

### 11.3 第一版目标

项目完整目标需要实现全部层级，但应按版本阶段逐步实现。

第一版 V2-lite 目标是层级 2，并具备部分层级 3 能力：

1. 能基于索引检索相关片段。
2. 能定位具体视频时间段。
3. 能概括片段内容。
4. 能说明为什么该片段支撑回答。
5. 必要时能回查转录文本核对文字细节。
6. 暂不强制支持截图、OCR、抽帧和完整画面理解。

## 12. 第一版范围

### 12.1 版本定位

第一个真正可交付版本定为 V2-lite。

V2-lite 需要达到最基本的实用价值。它的重点不是只生成摘要，而是完成本地视频到索引再到追问的最小闭环。

### 12.2 V2-lite 必须支持

1. 输入本地视频文件。
2. 支持常见格式：mp4、mov、mkv。
3. 自动抽取音频。
4. 自动生成转录文本。
5. 转录文本保留时间戳。
6. 自动生成 Markdown 简明笔记。
7. 自动生成 YAML 主索引。
8. 自动导出 JSON 索引。
9. 支持基于索引追问。
10. 能定位相关片段。
11. 回答时说明片段内容和支撑理由。
12. 必要时回查转录文本核对文字细节。
13. 支持 1 到 3 小时长视频的分段处理。
14. 保存处理日志和失败记录。

### 12.3 V2-lite 暂不强制支持

1. 完整画面理解。
2. OCR。
3. 抽帧分析。
4. 自动回看原视频画面。
5. 多视频对比。
6. 长期视频库。
7. 多设备迁移。
8. 本地应用化。
9. 索引自进化的完整自动化。
10. 所有在线视频平台链接的自动处理。

## 13. 技术路线

### 13.1 运行环境

第一版优先在 Windows 加 NVIDIA 显卡环境跑通。

当前设备情况：

| 设备 | 第一版定位 |
|---|---|
| 集显设备 | 查看结果、编辑笔记、轻量追问、跑简单脚本 |
| 4070 Ti 设备 | 主力转录、长视频处理、批量测试 |
| 5060 设备 | 辅助转录、兼容性测试、备用处理环境 |

第一版先在 4070 Ti 设备完整跑通。后续版本再扩展多机可迁移。多机协同根据实际使用情况再判断。

### 13.2 交互方式

第一版采用命令行脚本加 Cursor 或 Claude Code Agent 调用。

底层命令行脚本示例：

```bash
python process_video.py path/to/video.mp4
```

脚本负责：

1. 检查视频文件。
2. 抽取音频。
3. 调用本地转录工具。
4. 生成带时间戳的转录文本。
5. 生成结构化索引基础文件。
6. 输出处理日志和结果路径。

Cursor 或 Claude Code Agent 负责：

1. 读取转录文本和索引文件。
2. 根据规则生成 Markdown 笔记。
3. 完善 YAML 索引。
4. 导出或校验 JSON 索引。
5. 支持用户基于索引追问。
6. 必要时回查转录文本。
7. 把追问中的新增信息沉淀到索引。

### 13.3 转录方案

转录方案采用开源免费优先、效果测试先行、API 兜底。

优先级如下：

| 优先级 | 方案 | 定位 |
|---|---|---|
| P0 | 开源、免费、可靠的本地工具 | 第一选择，需要做效果测试 |
| P1 | 公司提供的 Codex 环境或可调用的大模型 API | 免费开源方案效果不足时使用 |
| P2 | 用户自费工具 | 最后兜底 |

第一版重点测试：

1. faster-whisper
2. whisper.cpp
3. FFmpeg

faster-whisper 更适合 Windows 加 NVIDIA GPU。whisper.cpp 跨平台友好，后续迁移到 Mac Apple Silicon 更方便。FFmpeg 作为基础依赖，用于抽取音频、切分长视频和统一音频格式。

### 13.4 工具链可替换设计

转录层应设计成可替换模块：

```text
本地开源转录工具
↓ 效果不足时
公司可用模型或 API
↓ 仍不足时
用户自费工具
```

上层的视频索引和追问逻辑不应强绑定某一个转录工具。

## 14. 在线视频调研结论

在线视频链接自动分析可做，但不能把所有公开视频链接自动分析作为 V2-lite 刚性承诺。

原因包括：

1. 平台页面和接口变化会影响稳定性。
2. YouTube 等平台的官方字幕 API 有权限限制。
3. 字幕、登录、地区、反爬、cookie 和 headers 等因素会造成不稳定。
4. 部分内容受到 DRM、会员、付费墙、登录权限或私密权限限制。

V2-lite 对在线视频的合理目标是：

1. 能识别视频链接来源。
2. 尝试获取标题、简介、章节、字幕和音频。
3. 成功则进入自动分析。
4. 失败则明确告诉用户缺什么。
5. 支持用户补充本地视频、音频、字幕或转录文本继续处理。
6. 不绕过会员、私密、DRM、付费墙或未授权访问限制。

## 15. 语言范围

V2-lite 第一版语言范围如下：

| 语言类型 | 优先级 |
|---|---|
| 中文普通话 | P0 |
| 英文 | P0 |
| 中英混合 | P0 |
| 日语、韩语等其他语言 | P2 |
| 多人混杂、重口音、强噪声 | 压力测试，不作为首版强验收 |

## 16. 失败处理

### 16.1 失败处理原则

V2-lite 第一版不追求所有失败自动修复。合理要求是：

1. 能识别失败类型。
2. 能保留已完成的中间结果。
3. 能告诉用户下一步需要补什么。
4. 不能在依据不足时编造结论。
5. 能保存失败记录作为后续优化资料和依据。

### 16.2 失败类型与兜底方式

| 失败类型 | 处理方式 |
|---|---|
| 视频文件无法读取 | 输出错误原因，提示转码或换格式 |
| 音频抽取失败 | 尝试 FFmpeg 转码，再失败则停止 |
| 本地转录失败 | 降级模型、CPU 模式或分段重试 |
| 转录质量明显差 | 标记低质量转录，建议 API 或重新提供音频 |
| 时间戳错位严重 | 保留文本，但提示不适合精确片段定位 |
| 索引生成不完整 | 保留已有结果，标记缺失章节 |
| 追问无法回答 | 明确说明索引中依据不足，并指出需要回查材料 |
| 外部资料不足 | 明确说明无法可靠补充，不用低质量来源硬答 |

### 16.3 失败记录归档

失败记录保存到 `logs/` 目录。

建议文件：

```text
logs/
  process_log.md
  failure_log.json
```

失败记录字段：

```yaml
failure_record:
  time:
  video_id:
  stage:
  error_type:
  error_message:
  input_file:
  attempted_fixes:
  remaining_problem:
  user_action_needed:
  optimization_notes:
```

用途：

1. 复盘常见失败点。
2. 判断哪个环节最需要优化。
3. 对比不同工具、模型、参数的表现。
4. 为后续版本自动修复能力提供依据。
5. 避免同类问题重复排查。

## 17. 安全与权限边界

### 17.1 基本原则

优先处理用户有权访问和分析的视频。

包括：

1. 本地视频。
2. 用户自己保存的视频。
3. 公开视频。
4. 用户有合法访问权限的课程、会议、内部资料。

### 17.2 直接自动处理不可行时的原则

对于无法直接自动处理的目标视频，应在合法授权和平台规则允许范围内尽可能推进任务，给出可执行行动方案，并由用户判断和批准。

行动方案可以包括：

1. 用户手动下载后提供本地文件。
2. 用户导出字幕或转录文本。
3. 用户提供音频文件。
4. 用户提供公开视频页面信息。
5. 用户提供合法可访问的片段材料。
6. 改为基于已有字幕、简介、章节、截图做低配分析。

### 17.3 禁止行为

1. 不擅自绕过 DRM。
2. 不擅自绕过付费墙。
3. 不擅自绕过私密权限。
4. 不擅自绕过登录限制。
5. 不设计规避平台限制的失败处理。
6. 对私人或公司内部视频，默认本地处理优先。
7. 需要调用外部 API 时，先让用户确认。

## 18. V2-lite 验收标准

### 18.1 输入可用

1. 能处理本地 mp4、mov、mkv。
2. 能处理 30 分钟到 3 小时视频。
3. 能在 4070 Ti 设备上稳定跑完。

### 18.2 转录可用

1. 能生成带时间戳的转录文本。
2. 中文、英文、中英混合视频可读。
3. 时间戳能支持片段定位。
4. 个别术语错误允许存在，但不能影响整体理解。

### 18.3 第一轮索引可用

1. 能输出 Markdown 笔记。
2. 能输出 YAML 主索引。
3. 能自动导出 JSON。
4. 能覆盖完整视频脉络。
5. 高价值片段有明确标记。

### 18.4 追问可用

1. 用户提出问题后，Agent 能从索引中找到相关片段。
2. 能给出时间段。
3. 能概括片段内容。
4. 能说明为什么该片段支撑回答。
5. 必要时能回查转录文本。

### 18.5 可靠性可用

1. 不编造视频中没有的内容。
2. 对不确定内容能提示。
3. 外部信息只在必要时引入。
4. 引入外部信息时优先可靠来源。
5. 视频说法可能有问题时，先忠实复述视频内容，再单独提示问题。

## 19. 版本路线图

| 版本 | 目标 | 能力范围 |
|---|---|---|
| V0 | 项目模板 | 目录结构、规则文件、Markdown 模板、YAML 模板、JSON 模板、人工驱动流程 |
| V1 | 自动建索引 | 自动整理材料、生成转录文本、生成 Markdown 笔记和结构化索引 |
| V2-lite | 基本追问可用 | 根据问题检索索引，定位片段，给出回答依据和支撑理由 |
| V2-full | 追问核对增强 | 追问时可回查转录文本并修正索引 |
| V3 | 画面信息增强 | 加入截图、OCR、抽帧、画面理解 |
| V4 | 索引自进化 | 追问后自动补充、修正索引和高价值标记 |
| V5 | 本地应用化 | 从开发环境迁移到本地脚本或本地应用，降低使用门槛 |

## 20. 后续暂缓事项

以下内容先预留兼容空间，不作为 V2-lite 首版交付要求：

1. 多视频对比。
2. 信息去重与合并。
3. 观点分歧整理。
4. 长期视频库。
5. 跨视频检索。
6. 多设备迁移。
7. 多机协同。
8. 网盘、Git、NAS 或共享目录同步。
9. 完整本地应用界面。
10. 所有公开视频平台自动适配。

最低兼容原则是：每个视频的索引结构要足够标准化。这样未来扩展到多视频对比、去重、跨视频检索时，不需要重做底层格式。

## 21. 当前调研结论

### 21.1 第一版应做什么

V2-lite 第一版应聚焦本地视频文件，完成从视频输入到自动转录、自动建索引、生成笔记、支持基于索引追问和片段定位的最小闭环。

### 21.2 第一版不应过早做什么

第一版不应过早投入复杂 UI、多平台在线视频适配、多视频知识库、完整画面理解和多设备协同。它们都会提高复杂度，但不会决定首版是否具备基本实用价值。

### 21.3 最重要的产品判断

这个项目的最低实用价值出现在 V2-lite，而不是普通的视频摘要工具阶段。

原因是用户的真实需求是高效率消化视频，并围绕视频内容持续追问。只有当 Agent 能基于索引定位片段并解释依据时，项目才开始满足核心使用场景。

### 21.4 第一阶段开发重点

第一阶段应优先完成：

1. 项目目录结构。
2. 本地视频处理脚本。
3. FFmpeg 音频抽取。
4. faster-whisper 或 whisper.cpp 转录测试。
5. 带时间戳转录文件。
6. YAML 主索引模板。
7. JSON 自动导出。
8. Markdown 笔记模板。
9. Cursor 或 Claude Code Agent 规则文件。
10. 基于索引追问的工作流。
11. 失败记录机制。
12. V2-lite 验收测试。

## 22. 建议的下一步

进入设计与落地阶段时，建议按以下顺序推进：

1. 定义项目目录结构。
2. 设计 `video_index.yaml` 字段。
3. 设计 `video_note.md` 模板。
4. 设计 `failure_log.json` 和 `process_log.md` 格式。
5. 编写 Cursor 或 Claude Code 规则文件。
6. 编写本地视频处理脚本。
7. 选择 3 到 5 个典型视频做测试。
8. 根据测试结果调整索引字段和工作流。
9. 固化 V2-lite 验收清单。
10. 再评估在线视频链接入口可行性。
